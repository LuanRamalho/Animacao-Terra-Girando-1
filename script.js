// This script is not required for the animation, but allows for speed control (optional)

const earth = document.querySelector('.earth');
let speed = 5; // Adjust the number (in seconds) to control rotation speed

earth.style.animationDuration = `${speed}s`;
